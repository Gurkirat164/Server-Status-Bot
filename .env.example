BOT_TOKEN=your_bot_token_here
CLIENT_ID=your_application_client_id_here
# Optional: set this while developing for instant guild-scoped command updates.
# Remove/leave blank to register commands globally (takes up to ~1hr to propagate).
GUILD_ID=
